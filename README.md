# pid_deconflict
A Python automation script for ANSA that detects disconnected FE bodies and ensures each body receives a unique NASTRAN property ID (PID). This prevents cross‑body PID sharing, improves model organization, and avoids solver‑side conflicts
